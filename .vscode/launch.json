{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: dev-leumi",
            "request": "launch",
            "command": "npm run dev-leumi",
            "cwd": "${workspaceFolder}"
        }
    ]
}